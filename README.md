# Web Player

> UI-TARS 기반 웹 원격 데스크톱 제어 시스템

[![Python](https://img.shields.io/badge/Python-3.10+-blue.svg)](https://www.python.org/)
[![FastAPI](https://img.shields.io/badge/FastAPI-0.104+-green.svg)](https://fastapi.tiangolo.com/)
[![License](https://img.shields.io/badge/License-MIT-yellow.svg)](LICENSE)

## 개요

**Web Player**는 웹 브라우저를 통해 원격 컴퓨터의 화면을 실시간으로 스트리밍하고, UI-TARS의 강력한 액션 파싱 기능을 활용하여 마우스/키보드 입력을 원격으로 전달하는 시스템입니다.

### 주요 기능

- **실시간 화면 스트리밍**: 30 FPS로 원격 화면을 웹 브라우저에 표시
- **마우스 제어**: 클릭, 더블클릭, 우클릭, 드래그 지원
- **키보드 제어**: 텍스트 입력 및 단축키(Ctrl+C, Alt+Tab 등) 지원
- **스크롤 지원**: 마우스 휠 스크롤 원격 전달
- **UI-TARS 통합**: 정확한 좌표 변환 및 액션 파싱
- **크로스 플랫폼**: Windows, macOS, Linux 지원

## 스크린샷

```
┌─────────────────────────────────────────────────────────────┐
│  🟢 Connected                           30 FPS    50 ms     │
├─────────────────────────────────────────────────────────────┤
│                                                             │
│                                                             │
│                   [원격 화면 표시]                          │
│                                                             │
│                                                             │
├─────────────────────────────────────────────────────────────┤
│  [Connect] [Disconnect] [Fullscreen]    Quality: ━━●━━ 70% │
└─────────────────────────────────────────────────────────────┘
```

## 빠른 시작

### 필수 요구사항

- Python 3.10 이상
- Git
- 지원 운영체제: Windows 10+, macOS 11+, Ubuntu 20.04+

### 설치

```bash
# 1. 레포지토리 클론
git clone git@github.com:seunghyun-kim-bagel/web-player.git
cd web-player

# 2. 가상환경 생성 및 활성화
python -m venv venv

# Windows:
venv\Scripts\activate

# macOS/Linux:
source venv/bin/activate

# 3. 의존성 설치
pip install -r requirements.txt

# 4. 환경 변수 설정 (선택사항)
cp .env.example .env
```

### 실행

```bash
# 서버 실행
python src/server/main.py

# 또는 uvicorn 직접 실행
uvicorn src.server.main:app --host 0.0.0.0 --port 8000 --reload
```

브라우저에서 접속:
```
http://localhost:8000
```

## 사용 방법

### 1. 서버 시작

```bash
python src/server/main.py
```

서버가 시작되면 다음과 같은 로그가 표시됩니다:
```
INFO:     Started server process
INFO:     Waiting for application startup.
INFO:     Application startup complete.
INFO:     Uvicorn running on http://0.0.0.0:8000
```

### 2. 웹 브라우저 접속

브라우저에서 `http://localhost:8000` 접속

### 3. 연결

- 자동으로 WebSocket 연결 시도
- 또는 "Connect" 버튼 클릭

### 4. 원격 제어

- **클릭**: Canvas를 클릭하면 원격 화면의 해당 위치가 클릭됨
- **더블클릭**: 빠르게 두 번 클릭
- **우클릭**: 우클릭으로 컨텍스트 메뉴 열기
- **드래그**: 마우스 버튼을 누른 상태로 이동
- **스크롤**: 마우스 휠로 스크롤
- **키보드**: 브라우저 창에서 키보드 입력

### 5. 단축키

- **Ctrl+C**: 복사
- **Ctrl+V**: 붙여넣기
- **Alt+Tab**: 창 전환
- **기타**: 대부분의 표준 단축키 지원

## 프로젝트 구조

```
web-player/
├── src/
│   ├── server/
│   │   ├── main.py                 # FastAPI 앱 진입점
│   │   ├── screen_controller.py    # 화면 캡처/스트리밍
│   │   ├── action_handler.py       # 액션 처리
│   │   ├── models.py                # 데이터 모델
│   │   └── config.py                # 설정
│   └── client/
├── static/
│   ├── css/
│   │   └── style.css
│   ├── js/
│   │   ├── websocket-client.js     # WebSocket 통신
│   │   ├── screen-renderer.js      # 화면 렌더링
│   │   ├── input-handler.js        # 입력 처리
│   │   └── main.js                 # 메인 앱
│   └── index.html
├── tests/
│   ├── test_screen_controller.py
│   ├── test_action_handler.py
│   └── test_integration.py
├── docs/
│   ├── SPEC.md                     # 기술 스펙
│   ├── ARCHITECTURE.md             # 아키텍처 설계
│   ├── DEVELOPMENT.md              # 개발 가이드
│   └── API.md                      # API 명세
├── requirements.txt
├── .env.example
├── .gitignore
└── README.md
```

## 기술 스택

### Backend
- **Python 3.10+**: 메인 언어
- **FastAPI**: Web 프레임워크
- **UI-TARS**: 액션 파싱 및 좌표 변환
- **PyAutoGUI**: 마우스/키보드 제어
- **MSS**: 화면 캡처
- **Pillow**: 이미지 처리

### Frontend
- **Vanilla JavaScript**: 프레임워크 없이 가볍게
- **HTML5 Canvas**: 화면 렌더링
- **WebSocket API**: 실시간 통신

### Communication
- **WebSocket**: 양방향 실시간 통신
- **JSON**: 데이터 포맷

## 아키텍처

```
┌─────────────────┐         WebSocket          ┌─────────────────┐
│  Web Browser    │◄─────────────────────────►│   Server        │
│                 │         (JSON)             │                 │
│  • Canvas       │                            │  • FastAPI      │
│  • WebSocket    │                            │  • UI-TARS      │
│  • Input        │                            │  • PyAutoGUI    │
└─────────────────┘                            └─────────────────┘
                                                        │
                                                        ▼
                                                ┌─────────────────┐
                                                │  OS (Win/Mac/   │
                                                │  Linux)         │
                                                └─────────────────┘
```

자세한 내용은 [ARCHITECTURE.md](docs/ARCHITECTURE.md) 참조

## 성능

| 메트릭 | 목표 | 설명 |
|--------|------|------|
| 화면 FPS | 30 FPS | 화면 스트리밍 프레임 속도 |
| 액션 지연 | < 100ms | 클릭 → 실행 시간 |
| 네트워크 지연 | < 500ms | 왕복 시간 (RTT) |
| CPU 사용률 | < 20% | 서버 유휴 시 |
| 메모리 | < 500MB | 서버 메모리 사용량 |
| 대역폭 | < 5 Mbps | 30 FPS 기준 |

## API

### WebSocket 엔드포인트

**URL**: `ws://localhost:8000/ws`

**메시지 타입**:
- `screen`: 화면 프레임 (Server → Client)
- `action`: 액션 요청 (Client → Server)
- `status`: 상태 메시지
- `error`: 에러 메시지

자세한 API 명세는 [API.md](docs/API.md) 참조

### 액션 예시

```javascript
// 클릭
{
    "type": "action",
    "action_type": "click",
    "x": 100,
    "y": 200
}

// 텍스트 입력
{
    "type": "action",
    "action_type": "type",
    "text": "Hello World"
}

// 단축키
{
    "type": "action",
    "action_type": "hotkey",
    "key": "ctrl c"
}
```

## 개발

### 개발 환경 설정

```bash
# 개발 의존성 설치
pip install -r requirements.txt

# 코드 포맷팅
black src/

# 린팅
flake8 src/

# 타입 체크
mypy src/
```

### 테스트

```bash
# 모든 테스트 실행
pytest tests/ -v

# 특정 테스트 실행
pytest tests/test_action_handler.py -v

# 커버리지 확인
pytest --cov=src tests/
```

### 디버깅

VS Code에서 F5를 눌러 디버거 시작

자세한 개발 가이드는 [DEVELOPMENT.md](docs/DEVELOPMENT.md) 참조

## 설정

### 환경 변수

`.env` 파일에서 설정:

```env
# Server
SERVER_HOST=0.0.0.0
SERVER_PORT=8000

# Screen Capture
SCREEN_FPS=30
SCREEN_QUALITY=70

# Security
ENABLE_AUTH=false
AUTH_TOKEN=your-secret-token

# Logging
LOG_LEVEL=INFO
```

### 커스터마이징

- **FPS 변경**: `SCREEN_FPS` 환경 변수 수정
- **품질 조정**: `SCREEN_QUALITY` (1-100)
- **인증 활성화**: `ENABLE_AUTH=true` 및 `AUTH_TOKEN` 설정

## 보안

### 주의사항

⚠️ **경고**: 이 시스템은 원격에서 컴퓨터를 완전히 제어할 수 있습니다. 프로덕션 환경에서는 반드시 다음 보안 조치를 취하세요:

1. **인증 활성화**: `ENABLE_AUTH=true` 설정
2. **HTTPS/WSS 사용**: SSL 인증서 적용
3. **방화벽 설정**: 필요한 포트만 열기
4. **강력한 토큰**: 충분히 긴 랜덤 토큰 사용
5. **Rate Limiting**: 과도한 요청 차단

### 프로덕션 배포

```bash
# SSL 인증서가 있는 경우
uvicorn src.server.main:app \
    --host 0.0.0.0 \
    --port 443 \
    --ssl-keyfile /path/to/privkey.pem \
    --ssl-certfile /path/to/fullchain.pem \
    --workers 4
```

## 문제 해결

### 자주 묻는 질문

**Q: 화면이 표시되지 않습니다**
- A: 서버 로그를 확인하고, 화면 캡처 권한이 있는지 확인하세요.

**Q: 클릭이 작동하지 않습니다**
- A: PyAutoGUI가 올바르게 설치되었는지, OS 보안 설정을 확인하세요.

**Q: macOS에서 권한 오류가 발생합니다**
- A: 시스템 환경설정 > 보안 및 개인 정보 보호 > 접근성에서 Python/Terminal 권한을 부여하세요.

**Q: 연결이 계속 끊깁니다**
- A: 네트워크 연결을 확인하고, 방화벽 설정을 검토하세요.

### 로그 확인

```bash
# 서버 로그
tail -f logs/server.log

# 디버그 모드로 실행
LOG_LEVEL=DEBUG python src/server/main.py
```

## 기여

기여는 언제나 환영합니다!

1. Fork the repository
2. Create your feature branch (`git checkout -b feature/amazing-feature`)
3. Commit your changes (`git commit -m 'Add some amazing feature'`)
4. Push to the branch (`git push origin feature/amazing-feature`)
5. Open a Pull Request

### 개발 가이드라인

- PEP 8 코딩 스타일 준수
- Type hints 사용
- 테스트 작성
- Docstrings 작성 (Google 스타일)
- 커밋 메시지는 명확하게

## 로드맵

### v1.0 (현재)
- [x] 기본 화면 스트리밍
- [x] 마우스/키보드 제어
- [x] UI-TARS 통합
- [x] WebSocket 통신

### v1.1 (계획)
- [ ] 다중 모니터 지원
- [ ] 클립보드 공유
- [ ] 파일 전송
- [ ] 성능 최적화

### v2.0 (향후)
- [ ] WebRTC 기반 스트리밍
- [ ] 다중 사용자 동시 접속
- [ ] 세션 녹화
- [ ] AI 자동 조작 (UI-TARS AI)

### v3.0 (비전)
- [ ] 모바일 앱 (iOS/Android)
- [ ] 게임 컨트롤 최적화
- [ ] VR/AR 지원

## 라이선스

이 프로젝트는 MIT 라이선스 하에 배포됩니다. 자세한 내용은 [LICENSE](LICENSE) 파일을 참조하세요.

## 크레딧

- **UI-TARS**: ByteDance의 오픈소스 프로젝트 ([GitHub](https://github.com/bytedance/UI-TARS))
- **FastAPI**: Sebastián Ramírez의 웹 프레임워크
- **PyAutoGUI**: Al Sweigart의 GUI 자동화 라이브러리

## 관련 프로젝트

- [UI-TARS](https://github.com/bytedance/UI-TARS) - Native GUI Agent
- [UI-TARS-desktop](https://github.com/bytedance/UI-TARS-desktop) - Desktop 버전
- [Midscene.js](https://github.com/web-infra-dev/Midscene) - 브라우저 자동화

## 연락처

프로젝트 관련 문의:
- GitHub Issues: [Issues](https://github.com/seunghyun-kim-bagel/web-player/issues)
- Email: seunghyun-kim-bagel@users.noreply.github.com

---

**⭐ 이 프로젝트가 유용하다면 Star를 눌러주세요!**

Made with ❤️ by [seunghyun-kim-bagel](https://github.com/seunghyun-kim-bagel)
